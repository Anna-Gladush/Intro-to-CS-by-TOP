# Задачи для подготовки к собеседованиям

<a href="https://app.yeahub.ru/tasks?page=1">Yeahub</a>

## Первый уровень

### Квадраты чисел (Squares Array)

Условие: Дано целое число n. Нужно вернуть массив квадратов чисел от 0 до n - 1.

Входные данные: n — целое число.

Выходные данные: Массив целых чисел.

Ограничения: 1 <= n <= 10000

Пример:

Вход: n = 5

Выход: [0, 1, 4, 9, 16]

### Переворот строки на месте (Reverse String In-Place)

Напишите функцию reverse_string(s), которая переворачивает входной массив символов s на месте без использования дополнительной памяти (O(1) extra space).

Примеры ввода и ожидаемого вывода:

---

Input: s = ["h","e","l","l","o"]

Output: ["o","l","l","e","h"]

---

Input: s = ["H","a","n","n","a","h"]

Output: ["h","a","n","n","a","H"]

---

Ограничения:
Массив s может содержать только английские буквы.

1 ≤ len(s) ≤ 10^5

Нельзя использовать дополнительный массив для хранения результата.

### Удаление нулей из массива (Remove Zeros)

Дан массив целых чисел. Напишите функцию, которая возвращает новый массив, содержащий все элементы исходного массива, кроме нулей, с сохранением порядка оставшихся элементов.

Входные данные:
nums — массив целых чисел (длина от 0 до 10⁴).

Выходные данные:
Новый массив целых чисел без нулей, порядок элементов сохранён.

Ограничения:

0 <= nums.length <= 10000

-10^6 <= nums[i] <= 10^6

Пример:

Вход: []
Выход: []

Вход: [0]
Выход: []

Вход: [1, 0, 0, 2]
Выход: [1, 2]

### Заглавные буквы слов в строке (Capitalize First Letter of Each Word)

Условие:

Напишите функцию, которая принимает строку из одного или нескольких слов, разделённых пробелами, и возвращает строку, в которой первая буква каждого слова сделана заглавной, а остальные буквы слова остаются без изменений.

Входные данные:

s — строка, содержащая слова, разделённые одним пробелом (может быть пустой)

Выходные данные:

Строка, где первая буква каждого слова — заглавная, остальные символы слова не изменяются

Ограничения:

0 <= длина s <= 10^4

Строка состоит из строчных и заглавных латинских букв и пробелов

Слова разделены ровно одним пробелом, без начальных/конечных пробелов (если не оговорено иначе в примерах)

Примеры:

Вход: "hello world";
Выход: "Hello World"

Вход: "already Capitalized";
Выход: "Already Capitalized"

Вход: "";
Выход: ""

Вход: "a";
Выход: "A"

### Каррированное сложение (Curried Add)

Условие:

Реализуйте функцию add(a), которая принимает одно число и возвращает другую функцию, принимающую второе число. Итоговый вызов возвращает сумму двух чисел.

Входные данные: два числа a и b, передаваемые через каррирование: add(a)(b)

Выходные данные: число — сумма a + b

Ограничения:

-10^6 <= a, b <= 10^6

a и b — целые числа

Пример:

Вход: add(3)(5)
Выход: 8

Вход: add(-1)(1)
Выход: 0

Вход: add(0)(0)
Выход: 0

### Форматирование числа с разделителями тысяч (Format Number With Thousands Separator)

Условие:
С сервера приходит цена товара как целое число (например, 12345678). Нужно написать функцию, которая преобразует это число в строку, где группы из трёх цифр (считая от конца числа) разделены апострофом '.

Входные данные:
price — целое число в диапазоне от 1 до 2^31 - 1.

Выходные данные:
Строка — то же число, но с апострофами между группами по три цифры, считая справа.

Ограничения:

1 <= price <= 2147483647

### Слова с заданным префиксом (Words With Given Prefix)

Условие:

Дана строка s, содержащая слова, разделённые пробелами, и строка prefix. Напишите функцию, которая возвращает список всех слов из s (в порядке их появления, с повторениями), которые начинаются с prefix.

Входные данные:

s — строка со словами, разделёнными одним пробелом (может быть пустой)

prefix — строка-префикс для поиска (может быть пустой строкой)

Выходные данные:

Массив/список строк — все слова из s, начинающиеся с prefix, в исходном порядке, включая повторы

Ограничения:

0 <= длина s <= 10^5

0 <= длина prefix <= 100

Слова состоят из строчных и заглавных латинских букв

Если prefix — пустая строка, все слова считаются подходящими

Пример:

Вход: s = "ab abc def abc xyz ace ab cab", prefix = "ab"
Выход: ["ab", "abc", "abc", "ab"]

Вход: s = "hello world", prefix = "z"
Выход: []

Вход: s = "", prefix = "a"
Выход: []

### Преобразование массива свойств в объект (Array to Object Mapping)

Условие
С бекенда приходит массив объектов. Каждый объект содержит два поля:

name — название свойства;

value — значение свойства.

Необходимо написать функцию, которая преобразует такой массив в один объект, где каждый name становится ключом, а соответствующий value становится значением.

Если в массиве несколько объектов с одинаковым name, нужно использовать значение из последнего такого объекта.

Входные данные
Массив объектов вида:

[
{ name: "width", value: 10 },
{ name: "height", value: 20 }
]
Выходные данные
Объект, где ключи — это значения поля name, а значения — это значения поля value.

Ограничения

0 <= arr.length <= 10 000
name — непустая строка
value — любое примитивное значение: number, string, boolean, null
Если name повторяется, используется последнее значение
Пример
Вход

[
{ name: "width", value: 10 },
{ name: "height", value: 20 }
]
Выход

{
width: 10,
height: 20
}

### Дополнение строки пробелами слева (Left Pad)

Напишите функцию leftPad, которая добавляет слева к строке пробелы. Функция принимает два аргумента: число, обозначающее минимальную длину результата, и строку, которую нужно дополнить, если её длина меньше.

Правила:

Если длина строки меньше указанной минимальной длины, добавляются пробелы слева

Если длина строки больше или равна указанной длине, строка возвращается без изменений

Функция должна возвращать новую строку, не изменяя исходную

Примеры:

leftPad(6, 'test') // -> " test" (2 пробела слева)
leftPad(10, 'hello') // -> " hello" (5 пробелов)
leftPad(3, 'test') // -> "test" (длина уже больше)
leftPad(0, 'abc') // -> "abc"
Ограничения:

Минимальная длина: 0 ≤ N ≤ 1000

Длина строки: 0 ≤ length ≤ 1000

Символы: любые

Время выполнения: O(N)

Память: O(N)

### Фильтрация числовых значений из массива объектов (Filter Numeric Values From Objects)

Условие:

Дан массив объектов, каждый из которых содержит поле value. Значение этого поля может быть числом или строкой. Напишите функцию, которая возвращает новый массив, содержащий только числовые значения поля value (строковые значения отбрасываются, даже если строка визуально похожа на число, например "42"). Порядок значений в результате должен совпадать с порядком объектов в исходном массиве.

Входные данные:

items — массив объектов вида { value: number | string } (длина от 0 до 10⁴)

Выходные данные:
Массив чисел — значения поля value, у которых тип действительно number (строки, даже числоподобные, исключаются).

Ограничения:

0 <= items.length <= 10000

value может быть числом (включая отрицательные и дробные) или строкой

Другие типы (null, boolean, undefined, объекты) в поле value не встречаются

Пример:

Вход: [{value: 1}, {value: "2"}, {value: 3.5}, {value: "abc"}]
Выход: [1, 3.5]

Вход: [{value: "10"}, {value: "20"}]
Выход: []

Вход: []
Выход: []

### Обратный словарь (Reverse Dictionary)

Напишите функцию reverseKeyValue(dictionary), которая принимает словарь и возвращает новый словарь, где каждое значение исходного словаря становится ключом, а исходный ключ — значением.

Пример:

data = {1: 2, 3: 2, 's': 'b'}
reverse_key_value(data) # -> {2: 3, 'b': 's'}
Замечания:

Если несколько ключей имеют одно и то же значение, новый словарь оставляет последнее совпадение.

Все ключи и значения в исходном словаре могут быть любых типов, поддерживаемых в качестве ключей словаря в языке.

### Форматирование списка имён (Format Name List)

Не начата

Напишите функцию list, которая принимает массив объектов с полем name и возвращает строку, отформатированную в виде списка имён, разделённых запятыми, за исключением двух последних имён, которые должны быть разделены амперсандом &.

Правила:

Если массив пустой, вернуть пустую строку

Если в массиве один элемент, вернуть только это имя

Если в массиве два элемента, вернуть "name1 & name2"

Если в массиве три и более элементов, все имена, кроме последних двух, разделяются запятыми, а последние два — амперсандом

Примеры:

list([{name:'Bart'}]) // -> "Bart"
list([{name:'Bart'}, {name: 'Lisa'}]) // -> "Bart & Lisa"
list([{name:'Bart'}, {name: 'Lisa'}, {name: 'Maggie'}]) // -> "Bart, Lisa & Maggie"
list([]) // -> ""
Ограничения:

Длина массива: 0 ≤ N ≤ 1000

Объекты всегда имеют поле name

Имена — строки

Время выполнения: O(N)

Память: O(N)

### Разница в возрасте (Difference in Ages)

Напишите функцию differenceInAges, которая принимает массив возрастов и возвращает массив из трёх чисел: самого младшего возраста, самого старшего возраста и разницы между ними (старший - младший).

Правила:

Массив всегда содержит хотя бы один элемент

Возраста — целые неотрицательные числа

Возвращается массив в формате [min, max, difference]

Разница вычисляется как max - min

Примеры:

differenceInAges([82, 15, 6, 38, 35]) // -> [6, 82, 76]
differenceInAges([57, 99, 14, 32]) // -> [14, 99, 85]
differenceInAges([25]) // -> [25, 25, 0]
differenceInAges([10, 10, 10]) // -> [10, 10, 0]
Ограничения:

Длина массива: 1 ≤ N ≤ 1000

Возраста: 0 ≤ age ≤ 200

Время выполнения: O(N)

Память: O(1)

### Поиск дубликатов в массиве (Find Duplicates)

Напишите функцию existsDuplicate, которая принимает массив целых чисел и возвращает true, если какое-либо значение появляется в массиве не менее двух раз, и false, если каждый элемент уникален.

Правила:

Массив содержит целые числа

Функция должна вернуть true при наличии хотя бы одного дубликата

Если все элементы уникальны, возвращается false

Пустой массив считается не содержащим дубликатов

Примеры:

existsDuplicate([4, 6, 7, 7, 1]) // -> true
existsDuplicate([7, 1, 5, 4, 2, 10]) // -> false
existsDuplicate([1, 2, 3, 1]) // -> true
existsDuplicate([]) // -> false
Ограничения:

Длина массива: 0 ≤ N ≤ 1000

Элементы: целые числа

Время выполнения: O(N)

Память: O(N)

### Футбольный приз (Football Prize)

Две команды, А и Б, играют в футбол. Некто делает свою ставку на результат матча, например "1:2". По окончании матча становится известен настоящий счёт, и нам надо выдать тот или иной приз:

Если некто угадал точный счёт — он получает большой приз (2)

Если некто угадал исход матча (победа/ничья/поражение) — он получает маленький приз (1)

Если же он не угадал — он получает нулевой приз (0)

Необходимо написать функцию, которая принимает в качестве аргументов предполагаемый счёт и реальный счёт, и возвращает целое число 0, 1 или 2.

Правила:

Счёт передаётся в формате "X:Y", где X — голы команды А, Y — голы команды Б

Точный счёт — совпадают оба числа

Исход матча определяется сравнением голов:

Победа команды А: X > Y

Победа команды Б: X < Y

Ничья: X = Y

Примеры:

getPrize('1:2', '1:2') // -> 2 (точный счёт)
getPrize('2:1', '5:0') // -> 1 (исход: победа А)
getPrize('3:0', '2:2') // -> 0 (не угадал)
Ограничения:

Строки всегда в формате "X:Y" с целыми неотрицательными числами

X и Y — целые числа от 0 до 100

Время выполнения: O(1)

Память: O(1)

### FizzBuzz

Напишите функцию fizzBuzz, которая выводит числа от 1 до 100. При этом:

Если число кратно 3, вместо числа выводится "Fizz"

Если число кратно 5, вместо числа выводится "Buzz"

Если число кратно и 3, и 5, вместо числа выводится "FizzBuzz"

В остальных случаях выводится само число

Функция должна вернуть строку, где все результаты разделены пробелами.

Примеры:

Первые 15 чисел:
Вход: (функция без параметров)
Выход: "1 2 Fizz 4 Buzz Fizz 7 8 Fizz Buzz 11 Fizz 13 14 FizzBuzz"

Конкретные проверки:
Число 3 → "Fizz"
Число 5 → "Buzz"  
Число 15 → "FizzBuzz"
Число 7 → "7"
Ограничения:

Функция не принимает параметров (всегда от 1 до 100)

Возвращает строку с пробелами между элементами

В конце строки пробела нет

### Сумма цифр числа (Sum of Digits)

Напишите функцию sumDigits, которая принимает целое число и возвращает сумму всех его цифр. Число может быть как положительным, так и отрицательным. Для отрицательных чисел сумма считается по модулю (знак минуса игнорируется).

Примеры:

Вход: 123
Выход: 6
Пояснение: 1 + 2 + 3 = 6

Вход: 904
Выход: 13
Пояснение: 9 + 0 + 4 = 13

Вход: -506
Выход: 11
Пояснение: | -506 | = 506 → 5 + 0 + 6 = 11

Вход: 0
Выход: 0
Ограничения:

Число может быть любым целым числом в диапазоне от -10^9 до 10^9

Для отрицательных чисел суммируются цифры абсолютного значения

Функция должна работать с числом, а не со строкой (можно преобразовывать внутри)

### Максимальное число в массиве (Maximum Number)

Напишите функцию findMax, которая принимает массив чисел и возвращает максимальное число из массива.

Примеры:

Вход: [1, 2, 5]
Выход: 5

Вход: [10, -5, 3, 8]
Выход: 10

Вход: [-1, -5, -3]
Выход: -1

Вход: [42]
Выход: 42
Ограничения:

Массив содержит только числа (целые или с плавающей точкой)

Массив всегда содержит хотя бы один элемент

Длина массива не превышает 1000 элементов

### Подсчет количества гласных (Count Vowels)

Напишите функцию countVowels, которая принимает строку и возвращает количество гласных букв (a, e, i, o, u) в этой строке. Функция должна игнорировать регистр букв.

Примеры:

Вход: "hello"
Выход: 2

Вход: "world"
Выход: 1

Вход: "AEIOU"
Выход: 5

Вход: "xyz"
Выход: 0
Ограничения:

Строка может содержать буквы в любом регистре (верхнем или нижнем)

Строка может быть пустой (тогда возвращается 0)

Строка может содержать только латинские буквы

Гласными считаются только символы a, e, i, o, u (и их заглавные аналоги)

### Сумма положительных нечетных чисел (Sum of Positive Odd Numbers)

Напишите функцию sumPositiveOdd, которая принимает массив целых чисел и возвращает сумму всех нечетных чисел, которые больше нуля.

Примеры:

Вход: [5, 0, -5, 20, 88, 17, -32]
Выход: 22
Пояснение: 5 + 17 = 22

Вход: [1, 3, 5, 7]
Выход: 16
Пояснение: 1 + 3 + 5 + 7 = 16

Вход: [2, 4, 6, 8]
Выход: 0
Пояснение: Нет нечетных чисел

Вход: [-1, -3, -5]
Выход: 0
Пояснение: Нет положительных чисел
Ограничения:

Массив может содержать целые числа (положительные, отрицательные, ноль)

Массив может быть пустым (тогда сумма равна 0)

Нечетными считаются числа, которые не делятся на 2 нацело

Положительными считаются числа больше 0
